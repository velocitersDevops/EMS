/**
 * In this file we are configure important information which we will apply in multiple condition
 */
limitForCondition = {
  // This can have nested stuff, arrays, etc.
  projectLimit: 1,
  projectzero:0,
  passwordMaxLmit:12,
  passwordMinLmit:8,
  mobileNumberLimit:10,
}